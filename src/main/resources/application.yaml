spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/clinicaoitavarosado
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
    show-sql: true
    open-in-view: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: francinaldomanoel135@gmail.com
    password: auly axut dymt yqls
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      
server:
  port: 8080

security:
  jwt:
    token:
      secret-key: 53cr37
      expire-length: 3600000
